# ==============================================================================
# Configuración de la API de Bizuit BPM - SDK v2.0.0+
# ==============================================================================
#
# IMPORTANTE: Con SDK v2.0.0+ solo necesitas UNA variable de entorno
#
# cp .env.example .env.local
#
# ==============================================================================

# URL base de la API de Bizuit (sin /forms o /dashboard al final)
# El SDK v2.0.0+ automáticamente construye las rutas necesarias
#
# Para desarrollo local con proxy de Next.js (evita problemas de CORS):
NEXT_PUBLIC_BIZUIT_API_URL=/api/bizuit

# Para producción o si el backend tiene CORS configurado:
# NEXT_PUBLIC_BIZUIT_API_URL=https://test.bizuit.com/arielschbizuitdashboardapi/api

# ==============================================================================
# Configuración Opcional
# ==============================================================================

# Timeout para peticiones HTTP en milisegundos (por defecto: 30000 = 30 segundos)
# NEXT_PUBLIC_BIZUIT_TIMEOUT=30000

# Token expiration time in minutes (default: 1440 minutes = 24 hours)
# NEXT_PUBLIC_BIZUIT_TOKEN_EXPIRATION_MINUTES=1440

# Internal configuration for proxy (server-side only)
# Esta URL se usa en el proxy de Next.js para redirigir /api/bizuit → URL real
# BIZUIT_API_BASE_URL=https://test.bizuit.com/arielschbizuitdashboardapi/api

# ==============================================================================
# Configuración de Desarrollo
# ==============================================================================
# Si estás usando Bizuit BPM en tu máquina local, usa esta URL:
#
# NEXT_PUBLIC_BIZUIT_API_URL=http://localhost:5000/api

# ==============================================================================
# Notas
# ==============================================================================
#
# 1. SDK v2.0.0+ simplificó la configuración a una sola URL base
# 2. Las variables deben empezar con NEXT_PUBLIC_ para ser accesibles en el cliente
# 3. Después de cambiar estas variables, reinicia el servidor de desarrollo (npm run dev)
# 4. En producción, configura estas variables en tu plataforma de hosting
# 5. NUNCA subas el archivo .env.local al repositorio git
#
