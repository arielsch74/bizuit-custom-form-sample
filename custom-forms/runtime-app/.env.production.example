# ==============================================================================
# BIZUIT Custom Forms - Runtime App Production Environment Variables
# ==============================================================================
#
# IMPORTANTE: Este es un archivo de EJEMPLO.
# NO contiene valores reales - solo placeholders.
#
# Para configurar producción:
# 1. Copiar este archivo como .env.local en el servidor
# 2. Reemplazar los placeholders con valores reales
# 3. NUNCA commitear .env.local al repositorio
#
# ==============================================================================

# ==============================================================================
# Build-Time Variables (NEXT_PUBLIC_*)
# ==============================================================================
# Estas variables se "bakean" en el build de Next.js y NO se pueden cambiar
# sin rebuild. Se configuran en el pipeline de Azure DevOps.
#
# IMPORTANTE: Cambiar estas variables requiere rebuild completo

# Base path para deployment en subdirectorio
# Ejemplo: /arielschBIZUITCustomForms
NEXT_PUBLIC_BASE_PATH=/CAMBIAR_BASE_PATH_AQUI

# URL del API de Forms (backend FastAPI)
# Para desarrollo local con proxy: /api/bizuit
# Para producción con proxy IIS: /api/bizuit
NEXT_PUBLIC_BIZUIT_FORMS_API_URL=/api/bizuit

# URL del API de Dashboard de BIZUIT
# Para desarrollo local con proxy: /api/bizuit
# Para producción con proxy IIS: /api/bizuit
NEXT_PUBLIC_BIZUIT_DASHBOARD_API_URL=/api/bizuit

# ==============================================================================
# Runtime Variables (Server-Side)
# ==============================================================================
# Estas variables se leen en el servidor y se pueden cambiar sin rebuild
# (solo reiniciar la app con PM2)

# URL del backend FastAPI (interno, sin proxy)
# Ejemplo: http://localhost:8000
FASTAPI_URL=http://localhost:8000

# Webhook secret para endpoint /api/forms/reload
# Generar con: openssl rand -hex 32
# CAMBIAR EN PRODUCCIÓN
WEBHOOK_SECRET=CAMBIAR_WEBHOOK_SECRET_AQUI

# Session timeout en minutos (debe coincidir con backend)
# Default: 30 minutos
NEXT_PUBLIC_SESSION_TIMEOUT_MINUTES=30

# ==============================================================================
# Custom Forms System (Dynamic Form Loading)
# ==============================================================================
# URL del Custom Forms API (backend .NET Core) - si está implementado
# Si no está implementado, dejar comentado o vacío
# Ejemplo: https://test.bizuit.com/api/custom-forms
# NEXT_PUBLIC_CUSTOM_FORMS_API_URL=

# ==============================================================================
# Opcional: Configuración de Timeouts
# ==============================================================================
# Timeout para peticiones HTTP en milisegundos
# Default: 30000 (30 segundos)
# NEXT_PUBLIC_BIZUIT_TIMEOUT=30000

# ==============================================================================
# Notas Importantes
# ==============================================================================
#
# 1. Variables NEXT_PUBLIC_* son accesibles en el cliente (browser)
# 2. Variables sin prefijo son solo accesibles en el servidor
# 3. Después de cambiar variables NEXT_PUBLIC_*, ejecutar:
#    - npm run build (rebuild completo)
#    - pm2 restart arielsch-runtime
# 4. Después de cambiar variables sin prefijo:
#    - pm2 restart arielsch-runtime (no requiere rebuild)
# 5. NUNCA subir valores reales al repositorio git
# 6. Mantener este archivo sincronizado con .env.example para desarrollo
#
