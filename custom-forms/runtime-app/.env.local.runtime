# Runtime configuration for basePath testing
# This allows changing basePath without rebuilding

# Runtime basePath (can be changed without rebuild!)
BASE_PATH=/arielschBIZUITCustomForms

# Backend API URL
FASTAPI_URL=http://localhost:8000

# Dashboard API URL
NEXT_PUBLIC_BIZUIT_DASHBOARD_API_URL=https://test.bizuit.com/arielschbizuitdashboardapi/api

# Other public configs (still build-time)
NEXT_PUBLIC_BIZUIT_TIMEOUT=60000
NEXT_PUBLIC_BIZUIT_TOKEN_EXPIRATION_MINUTES=30
NEXT_PUBLIC_ALLOW_DEV_MODE=true