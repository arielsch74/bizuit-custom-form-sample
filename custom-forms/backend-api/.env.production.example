# ==============================================================================
# BIZUIT Custom Forms - Backend API Production Environment Variables
# ==============================================================================
#
# IMPORTANTE: Este es un archivo de EJEMPLO.
# NO contiene valores reales - solo placeholders.
#
# Para configurar producción:
# 1. Copiar este archivo como .env.local en el servidor
# 2. Reemplazar los placeholders con valores reales
# 3. NUNCA commitear .env.local al repositorio
#
# ==============================================================================

# ==============================================================================
# SQL Server Connection - Dashboard Database
# ==============================================================================
# NOTE: El código usa "ODBC Driver 17 for SQL Server" por defecto.
# Si necesitas Driver 18, actualizar database.py get_connection_string()

DB_SERVER=CAMBIAR_SQL_SERVER_AQUI
DB_DATABASE=CAMBIAR_DATABASE_NAME_AQUI
DB_USER=CAMBIAR_DATABASE_USER_AQUI
DB_PASSWORD=CAMBIAR_DATABASE_PASSWORD_AQUI

# ==============================================================================
# SQL Server Connection - Persistence Store Database (Token Validation)
# ==============================================================================
PERSISTENCE_DB_SERVER=CAMBIAR_PERSISTENCE_SQL_SERVER_AQUI
PERSISTENCE_DB_DATABASE=CAMBIAR_PERSISTENCE_DATABASE_NAME_AQUI
PERSISTENCE_DB_USER=CAMBIAR_PERSISTENCE_USER_AQUI
PERSISTENCE_DB_PASSWORD=CAMBIAR_PERSISTENCE_PASSWORD_AQUI

# ==============================================================================
# Bizuit Dashboard API (for authentication)
# ==============================================================================
# URL del API del Dashboard de BIZUIT para autenticación
# Ejemplo: https://test.bizuit.com/arielschbizuitdashboardapi/api
BIZUIT_DASHBOARD_API_URL=CAMBIAR_DASHBOARD_API_URL_AQUI

# ==============================================================================
# Admin Security Configuration
# ==============================================================================
# Lista de roles separados por comas que pueden acceder al panel admin
# Ejemplo: Administrators,BIZUIT Admins,SuperAdmin,FormManager
ADMIN_ALLOWED_ROLES=CAMBIAR_ROLES_PERMITIDOS_AQUI

# Session timeout en minutos (default: 30)
SESSION_TIMEOUT_MINUTES=30

# JWT Secret Key para session tokens
# IMPORTANTE: Generar con: openssl rand -hex 32
# NUNCA usar el mismo secret en múltiples ambientes
JWT_SECRET_KEY=CAMBIAR_JWT_SECRET_KEY_AQUI

# TripleDES Encryption Key para validación de tokens del Dashboard
# IMPORTANTE: Debe ser exactamente 24 caracteres
# CRÍTICO: Debe coincidir con la key usada por Bizuit Dashboard
# Contactar al administrador de Bizuit Dashboard para obtener la key correcta
ENCRYPTION_TOKEN_KEY=CAMBIAR_24_CHAR_KEY_AQUI

# ==============================================================================
# API Configuration
# ==============================================================================
# Puerto del API (PM2 usa este valor)
API_PORT=8000

# Tamaño máximo de upload en MB
MAX_UPLOAD_SIZE_MB=50

# Directorio temporal para uploads
TEMP_UPLOAD_PATH=./temp-uploads

# ==============================================================================
# CORS Configuration
# ==============================================================================
# Orígenes permitidos para CORS (separados por comas)
# Ejemplo: https://test.bizuit.com,https://prod.bizuit.com
CORS_ORIGINS=CAMBIAR_CORS_ORIGINS_AQUI

# ==============================================================================
# Production Mode
# ==============================================================================
# Indicador de ambiente de producción (desactiva reload, activa logs de producción)
PYTHON_ENV=production

# ==============================================================================
# Notas Importantes
# ==============================================================================
#
# 1. DB_PASSWORD y PERSISTENCE_DB_PASSWORD deben ser passwords reales
# 2. JWT_SECRET_KEY debe ser único por ambiente (generar con openssl rand -hex 32)
# 3. ENCRYPTION_TOKEN_KEY debe coincidir EXACTAMENTE con Bizuit Dashboard
# 4. ADMIN_ALLOWED_ROLES debe incluir los roles exactos de BIZUIT BPM
# 5. CORS_ORIGINS debe incluir TODOS los dominios que accederán al API
# 6. Después de cambiar variables, ejecutar:
#    pm2 restart arielsch-backend
# 7. NUNCA subir este archivo con valores reales al repositorio git
# 8. Mantener sincronizado con .env.example para desarrollo
# 9. Para debugging, verificar conexión con database.py test_connection()
#
